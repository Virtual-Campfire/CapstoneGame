using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FMODInstance : MonoBehaviour
{
    // Start is called before the first frame update

   // FMOD.Studio.EventInstance AE;
    void Start()
    {
        //AE = FMODUnity.RuntimeManager.CreateInstance("Event;/BGMs/level1Combat");
      //  AE.start();
      //  AE.release();
    }
}
